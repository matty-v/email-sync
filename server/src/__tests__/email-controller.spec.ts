import { beforeAll, describe, expect, test } from '@jest/globals';

beforeAll(() => {
  // Mock out the gmail client
});

describe('Get emails by label', () => {
  test('will fetch emails with a label', async () => {
    expect(true).toBeDefined();
  });
});
